[project]
name = "sample-package"
version = "0.0.0"
description = ""
authors = []
readme = "README.md"
requires-python = "^3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pydocstyle]
match = "nothing"
